{
  "name": "JupyterLab with LLAMA API",
  "image": "python:3.10.13",
  "postCreateCommand": "pip install flask && pip install werkzeug && pip install requests && pip install python-dotenv && pip install aiohttp && pip install hypercorn && pip install transformers && pip install jupyter && pip install jupyterlab && pip install torch && pip install torchvision && pip install torchaudio && pip install diffusers && pip install transformers && pip install accelerate && pip install scipy && pip install ipywidgets && pip install tqdm && pip install redis && apt-get update && apt-get install python3-pip && chmod +x ./install.ash && ./install.sh",
  "customizations": {
      "vscode": {
          "extensions": [
              "ms-python.python",
              "ms-toolsai.jupyter",
              "jupyter.jupyterlab"
          ]
      }
  },
  "forwardPorts": [
      8888,
      9898

  ]
}
